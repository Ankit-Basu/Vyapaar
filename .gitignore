# --- python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- node / next ---
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# --- local state (regenerated by POST /demo/reset) ---
data/
*.db
*.db-wal
*.db-shm

# --- secrets ---
.env
.env.local
.env.*.local
!.env.example

# --- editors / os ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- next.js generated agent guide (regenerated by `next dev`) ---
apps/web/CLAUDE.md
apps/web/AGENTS.md

# Presentation notes. The video script is how the project gets *pitched*,
# not how it works — it is for the author's screen while recording, and it
# has no business in a repo a judge reads.
docs/VIDEO_PITCH.md
docs/SPOKEN_SCRIPT.md
