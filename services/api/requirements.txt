# --- Vyapaar API: core runtime ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- signed mandates (AP2-style) ---
PyJWT==2.10.1

# --- payments (Razorpay test mode) ---
# 2.x resolves its own version through importlib.metadata; 1.4.2 imported
# pkg_resources unconditionally, which Python 3.12+ no longer ships in a
# fresh venv -- so the live gateway crashed on import the moment real keys
# were configured.
razorpay==2.0.1
httpx==0.28.1

# --- vector math for catalog semantic search ---
numpy==2.2.1

# --- MCP server: lets any external agent transact with the merchant ---
mcp==1.2.0
