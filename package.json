{
  "name": "vyapaar",
  "version": "0.1.0",
  "private": true,
  "description": "An agent commerce layer: makes a Razorpay test-mode merchant discoverable and transactable by an external AI buyer agent.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "start": "npm run start --workspace apps/web",
    "lint": "npm run lint --workspace apps/web"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0"
  }
}
